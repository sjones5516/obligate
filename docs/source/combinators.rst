Combinators
======================

Boolean operators over the contract types defined in :doc:`validators`.
Each takes contracts of one type and returns a fresh contract of that same
type, so results compose further and drop in anywhere a plain contract is
expected.

.. automodule:: obligate.contracts.combinators
    :members:
